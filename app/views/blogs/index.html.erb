<div class="container">
  <h1 class="mt-5">My Blog</h1>
  <hr>
</div>

<div class="container blog-container bg-tranparent">
  <%= render "shared/blog_masthead" %>

  <div class="mt-4">
    <div class="row">
      <div class="col-md-8">
        <div class="blog-main">
          <%= paginate @blogs %>

          <%= render @blogs %>

          <%= paginate @blogs %>
        </div>
      </div>

      <div class="col-md-4">
        <%= render "shared/blog_sidebar" %>
      </div>
    </div>
  </div>
</div>